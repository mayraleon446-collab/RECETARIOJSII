import View from './View.js';
class RecipeView extends View{_parentElement=document.querySelector('.recipe');_errorMessage='No pudimos encontrar esa receta.';addHandlerRender(h){['hashchange','load'].forEach(e=>window.addEventListener(e,h));}_generateMarkup(){const r=this._data;return `<article class="recipe-card"><figure class="recipe-hero"><img src="${r.image}" alt="${r.title}" class="recipe-hero__img"><div class="recipe-hero__shade"></div><div class="recipe-hero__title"><span>RECETA / ${r.id.slice(-5).toUpperCase()}</span><h2>${r.title}</h2><p>${r.publisher}</p></div></figure><div class="recipe-meta"><div class="meta-item"><span>⏱</span><div><small>TIEMPO</small><strong>${r.cookTime} min</strong></div></div><div class="meta-item"><span>♙</span><div><small>PORCIONES</small><strong>${r.servings}</strong></div></div><a class="source-btn" href="${r.sourceUrl}" target="_blank">Ver receta ↗</a></div><section class="ingredients-section"><div class="section-heading"><span class="eyebrow">INGREDIENTES</span><h3>Todo lo que necesitas.</h3></div><ul class="ingredients-grid">${r.ingredients.map(i=>`<li class="ingredient"><span class="ingredient__check">✓</span><div><strong>${i.quantity??''} ${i.unit??''}</strong><span>${i.description}</span></div></li>`).join('')}</ul></section></article>`;}}
export default new RecipeView();
